{
  "name": "instadapp-indexer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ instadapp-indexer",
    "create-local": "graph create --node http://localhost:8020/ instadapp-indexer",
    "remove-local": "graph remove --node http://localhost:8020/ instadapp-indexer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 instadapp-indexer"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
